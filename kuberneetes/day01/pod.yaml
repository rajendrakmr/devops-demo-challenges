apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: nginx-namespace
  labels:
    app: nginx-app
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80

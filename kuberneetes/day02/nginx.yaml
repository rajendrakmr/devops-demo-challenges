apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: pv-namespace
spec:
  volumes:
    - name: nginx-storage
      persistentVolumeClaim:
        claimName: pvc
  containers:
    - name: nginx-container
      image: nginx
      ports:
        - containerPort: 80
      volumeMounts:
        - mountPath: "/usr/share/nginx/html"
          name: nginx-storage
